<% @product.reviews.each do |item| %>
  <% user = User.find_by_id(item.user_id) %>
  <dl class="dl-horizontal fas-star">
    <dd>
      <% item.rating.times do %>
        <%= fa_icon "star" %>
      <% end %>
    </dd>
    <dt><%= user.first_name %></dt>
    <dd><%= item.description %></dd>
    <hr>
  </dl>
<% end %>